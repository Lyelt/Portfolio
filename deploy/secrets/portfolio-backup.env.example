# Copy to /srv/secrets/portfolio/backup.env inside the VM.
# Generate a dedicated age identity. Store the secret identity in a recovery system outside the VM.
BACKUP_AGE_IDENTITY=replace-with-age-secret-key
BACKUP_AGE_RECIPIENT=replace-with-matching-age-recipient
