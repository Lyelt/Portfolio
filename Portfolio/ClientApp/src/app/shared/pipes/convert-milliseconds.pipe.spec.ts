import { ConvertMillisecondsPipe } from './convert-milliseconds.pipe';

describe('ConvertMillisecondsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertMillisecondsPipe();
    expect(pipe).toBeTruthy();
  });
});
